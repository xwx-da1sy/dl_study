# 2026-08-19

## 正则化技术教学 notebook（新增）
- 创建 `vision_transformer_practice/06_正则化技术_EarlyStopping与LabelSmoothing.ipynb`：11 cell（6 md + 5 code），讲 Early Stopping 与 Label Smoothing 两个正则化技术。
- 内容：Early Stopping 原理（val_loss U 形 + patience/min_delta 参数）+ 工程实现（training.py 早停逻辑）+ Label Smoothing 原理（soft label 公式 + α 选择）+ PyTorch 实现 + 5 个正则手段配合表（dropout/label smooth/early stop/增强/weight decay 各治什么）。
- 配图：示意 val_loss U 形曲线标早停点、hard vs soft 标签柱状对比、softmax 概率分析。
- 用 _build_reg_nb.py 生成后已删除该脚本。中文字体 SimHei。

## 数据增强教学 notebook（新增）
- 创建 `vision_transformer_practice/07_数据增强原理与实践.ipynb`：13 cell（6 md + 7 code），讲数据增强四大类（几何/像素/遮挡/自动搜索）+ 工程实现 + 关键原则。
- 配图：用真实 CIFAR-10 图展示 RandomCrop+Flip / ColorJitter / RandomErasing / RandAugment / 完整 train_transform 的变体网格。
- 对照 data.py 正则化版的 5 步流水线（Crop→Flip→RandAugment→ToTensor+Normalize→RandomErasing），含顺序原因（PIL 操作 vs Tensor 操作）。
- 用 _build_aug_nb.py 生成后已删除该脚本。

### 07 notebook 扩展（深入讲解两节）
- 在 07 notebook 增补两节深入讲解，从 13 cell 扩到 15 cell：
  - 第 4 节"为什么是这个顺序"：PIL vs Tensor 两种格式 + 每个变换的格式要求表 + 顺序被锁死的原因 + 报错验证代码
  - 第 5 节"为什么验证集不增强"：评估要可复现 + 模拟真实场景 + 增强破坏评估的例子 + 确定性预处理定义 + 训练/验证 transform 对照
- 中途插入脚本写坏了 notebook（code cells 丢失），用 _rebuild_aug_nb.py 重新生成完整 15 cell 版本修复，已删除脚本。

### 07 notebook 去 code cell
- 用户要求笔记里不要 code cells（纯阅读，不执行）。把 07 的 7 个 code cell 全转成 markdown，代码用 ```python 代码块包裹（不执行、纯展示）。
- 现在 07 是 15 个全 markdown cell 的纯讲解文档，不需要运行，直接读。
- 用 _convert_to_md.py 转换后已删除该脚本。

## 新项目：224×224 ViT 架构定稿（讨论，未写代码）
- 用户要在 `vision_transformer_practice/` 下新建子文件夹，复用 TinyViT 架构但输入从 32×32 改 224×224。
- 经讨论定稿（用户确认）：
  - 数据集：CIFAR-10 Resize 放大到 224×224（10 类，练手用，不追求比原 32×32 版更高准确率——放大不增加信息）。之前讨论过 ImageNet-100 但用户选了 CIFAR-10 放大练手路线（方案3）。
  - image_size=224, patch_size=16（→14×14=196 patches +CLS=197 序列）
  - embed_dim=256, num_heads=4（head_dim=64）, num_blocks=6, mlp_hidden=1024, num_classes=10, dropout=0.2
  - 参数量约 5.0M（Head 256→10=2.6K，比 ImageNet-100 版的 25.7K 小，总量基本不变）
  - 沿用正则化版全套（label_smooth 0.1 + early_stop patience20 + mixup α0.1 + RandAugment + RandomErasing + dropout 0.2）
- 沿用规则：生成内容前先读 .codex/skills 的 SKILL.md（deep-learning-notes + render-notebook-formulas），notebook 纯阅读无 code cell、公式 notebook 用 LaTeX/chat 用纯文本。
- 已建文件夹 vit_224/，复制原 vit/ 的 embedding/encoder/model/evaluation/training/__init__（逻辑不变），新写 config.py（224 参数）+ data.py（CIFAR-10 Resize 224 + RandomCrop 224 padding28 + 原增强）。checkpoint/results 用独立目录 checkpoints_224/results_224 避免和原 32×32 版冲突。
- 入口脚本 train_vit224.py / evaluate_vit224.py 写好（参照原 train_tiny_vit.py / evaluate_tiny_vit.py 改 import vit→vit_224 + 文字）。--help 验证通过，import + argparse OK。工程完整可跑：python train_vit224.py 开训。验证：参数量 4.99M，forward (2,3,224,224)->(2,10) 正确。
