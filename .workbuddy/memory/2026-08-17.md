# 2026-08-17

## ViT 工程梳理与改动
- 审查了 `vision_transformer_practice/`：4 个原理 notebook + `vit/` 包（config/data/embedding/encoder/model/training）+ `train_tiny_vit.py` 训练脚本 + `checkpoints/tiny_vit_best.pt`。
- 模型：从零 Tiny ViT on CIFAR-10。32×32 图、patch 4×4 → 64 token + CLS = 65；embed_dim 192、3 heads、4 EncoderBlock、MLP 768、dropout 0.1。
- 训练：AdamW(lr=3e-4, wd=0.05) + CosineAnnealingLR(→1e-6)，100 epoch，batch 128，grad clip 1.0，CUDA AMP，固定 seed=42，仅存 val 最佳。
- 新增 `.gitignore`（用户已 commit 并 push）：忽略 `data/`、`checkpoints/`、`__pycache__/`、venv、编辑器文件等。确认仓库此前未跟踪任何 .pt/.gz 大文件。
- 新增 `infer.py`：复用 `vit/` 包做推理。支持 `--image <path>` 单图预测（top-k）+ `--test-samples N` CIFAR-10 测试集抽样。自检通过（CPU 下加载检查点、forward、softmax top-3 正确）。
- 修复 `infer.py` bug：`main()` 中 `build_model(..., device=device)` 引用了未定义的 `device`，改为 `device=None` 由函数自动选择。

## 环境注意（重要）
- 本机 `D:\PythonWorkSpace\python.exe`：torch 2.6.0+cu124，CUDA 可用但**硬件不兼容**——RTX 5060 Laptop (sm_120 / compute capability 12.0) 超出该 torch 版本支持上限（最高 sm_90）。`torch.cuda.is_available()` 返回 True，但 forward/conv 实际报 "no kernel image is available"。
- 结论：本机 CUDA 实际不可用，训练与推理若要跑起来需升级 torch 到支持 sm_120 的版本（如 torch 2.7+/cu128 或 nightly），否则只能跑 CPU。
- **更新**：torch 已升级到 2.11.0+cu128（`D:\PythonWorkSpace\python.exe`），CUDA available，sm_120 兼容问题解决，evaluate 与 plot 脚本都能在 CUDA 上跑通，以上「只能跑 CPU」的结论作废。

## ViT 可视化分析（评估重跑后）
- 重跑 evaluate_tiny_vit.py 后，results/ 全套生成：training_history.json 实际 151 epoch（之前读到的 62 epoch 是 stale 数据，已被覆盖）。
- 真实数据：best val_acc @ ep144 = 0.8292（val_loss 0.7574, train_acc 0.9509, lr=2.44e-4）；test_acc = 0.8183（test_loss 0.6997）；最后 ep151 val_acc 0.8178 val_loss 0.782 train_acc 0.9555 lr=2.38e-4；min val_loss @ ep45 = 0.597（val_acc 0.804）。
- 之前「val 0.807 vs test 0.671 鸿沟 13.6pt 是核心问题」的判断被推翻：那是 best checkpoint 选择时机假象（62 epoch 时的 best 权重不成熟），现 test 0.8183 与 val 0.8292 几乎一致，泛化正常。
- 真正问题：过拟合。val_loss ep45 最低 0.597 → ep144 反弹到 0.757（+27%）；train_acc 0.951 vs val_acc 0.829 gap 12.2pt；min val_loss ep45 即过拟合起点。
- training_curves.png 已生成（1×3：loss / accuracy / lr），但无关键标注。增强清单：标 ep45(min val_loss/过拟合起点) + ep144(best val_acc) 两根竖虚线 + val_acc 末端叠 test 点 0.8183 + 三子图对齐 epoch 轴可合并双轴。
- best 选取准则（training.py:337 `validation_accuracy > best_validation_accuracy` 严格大于）→ 选了 val_acc 最高 ep144（但 val_loss 偏高）。从损失角度看 ep45 泛化可能更稳；可在 test 上对比两 checkpoint 验证。
- 训练 151/500 = 30%，lr 余弦退火从 3e-4 才到 2.38e-4（终点 1e-6 远未到），lr 子图应标 T_max=500 提示未跑满。
- 评估文件：confusion_matrix.png 已是行归一化（evaluation.py:247 row_totals），sample_predictions.png 取前 16 张（顺序），非误分类特写。per-class 全面提升：ship 0.897 automobile 0.911 horse 0.860 airplane 0.854 truck 0.849 frog 0.864 deer 0.799 bird 0.794 dog 0.768 cat 0.587（cat 仍最弱）。

## ViT 教学 notebook（新增）
- 创建 `vision_transformer_practice/05_训练曲线读图指南.ipynb`：14 cell（7 md + 7 code），教"看形状→找拐点→量鸿沟→看进度"四步读图法。读取 results/training_history.json 真实数据，画标注版训练曲线（ep_min_vl 过拟合起点竖虚线 / ep_best best 竖虚线 / test 点 × / 三阶段背景色 / train-val gap 双向箭头）。中文字体设 SimHei。用 _build_nb.py 生成后已删除该脚本。

## 性能评估三张图（新增）
- 创建 `vision_transformer_practice/plot_performance_evaluation.py`：加载 best ckpt（ep144）跑测试集，CUDA 加速，生成三张图存 results/。复用 vit 包的 create_cifar10_dataloaders + TinyViT。
- `per_class_accuracy.png`：每类按高到低柱状图，高于平均蓝、低于红，含平均虚线 81.7%。automobile 90.9 / ship 88.9 / truck 88.5 / frog 86.9 / horse 85.9 / airplane 84.5（蓝）vs bird 78.1 / deer 78.0 / dog 72.5 / cat 63.2（红）。
- `top_misclassifications.png`：混淆矩阵非对角 Top10 横向柱状图。cat↔dog 互为第一误分对各 14.2%（142张），CIFAR-10 经典猫狗难题；automobile↔truck 互混约 6%；cat→frog 5.3%、deer→bird 5.2%。
- `misclassified_samples.png`：16 张误分类样本 4×4 特写。关键观察：cat 类高置信度误分严重（cat→ship 96.9%、cat→truck 99.8%），是过拟合在概率校准上的表现；模型对"难类"（cat/dog/bird/deer）伤害大，"易类"（车船）几乎不受影响。

## 正则化版 vit 包替换（tiny_vit_regularized.zip）
- 用户提供的 `tiny_vit_regularized.zip` 包含一整套强化正则的 vit 包，已全部替换到 `vision_transformer_practice/vit/`（8 个 .py + CHANGES.txt），清理了 __pycache__。
- 改动对照（CHANGES.txt）：
  - config.py: DROPOUT_RATE 0.1→0.2, NUM_EPOCHS 500→150, 新增 LABEL_SMOOTHING=0.1 / EARLY_STOPPING_PATIENCE=20 / EARLY_STOPPING_MIN_DELTA=1e-4
  - data.py: 新增 RandAugment(num_ops=2, magnitude=9) + RandomErasing(p=0.25, scale=(0.02,0.20))；data.py 也新增了 CIFAR10_MEAN/STD 常量（evaluation.py 保留自己的副本，重复但不冲突）
  - training.py: CrossEntropyLoss 加 label_smoothing=0.1；CosineAnnealingLR T_max 跟随新 NUM_EPOCHS=150；Early Stopping 监控 val_loss(patience=20)；best checkpoint 改按最低 val_loss 保存（不再是 val_acc 最高）；fit() 返回值兼容
  - __init__.py: 导出新常量
  - embedding/encoder/model/evaluation: 未改动
- 导入验证通过：DROPOUT_RATE=0.2, NUM_EPOCHS=150, LABEL_SMOOTHING=0.1, EARLY_STOPPING_PATIENCE=20, WEIGHT_DECAY=0.05（未变）。train_tiny_vit.py / evaluate_tiny_vit.py / infer.py / plot_performance_evaluation.py 全部兼容，无需改动。
- 注意：旧 checkpoint tiny_vit_best.pt 是 dropout 0.1 训的，参数 shape 没变（dropout 不改参数）所以加载不会报错，但语义不匹配，应从头重训。旧 training_history.json / evaluation_summary.json / results 里的图是旧训练结果，重训会覆盖。
